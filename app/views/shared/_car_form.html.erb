<%= form_for(@cars) do |f| %>
  <%= render 'shared/error_messages', object: f.object %>
  <div class="field">
    <%= f.text_area :name%>
    <%= f.text_area :year %>
    <%= f.text_area :color %>
    <%= f.text_area :price %>
  </div>
  <%= f.submit "Post", class: "btn btn-primary" %>
<% end %>