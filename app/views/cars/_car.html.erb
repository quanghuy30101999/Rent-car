<li id="cars-<%= car.id %>">
  <span class="name">Name : <%= car.name %></span><br>
  <span class="year">Year  : <%= car.year %></span><br>
  <span>Price :<%= format_currency(car.price, car.price_unit) %></span><br>
  <span class="color">Color : <%= car.color %></span><br>
  <span class="timestamp">
    <% if logged_in? && admin? %>
      Posted <%= time_ago_in_words(car.created_at) %> ago.
        <%= link_to "edit", edit_car_url(car) %>
        <%= link_to "delete", car, method: :delete,
                                   data: { confirm: "You sure?" } %>
    <% end %>
  </span>
</li>
<hr>